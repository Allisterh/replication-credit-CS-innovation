glo data ../data
glo code ../code

/*
The code folder is usually the working directory. But because I have to unzip files and I can only do that in
the current dirrectory, I need to change the working directory the data folder and then change it back to the
code folder.
*/